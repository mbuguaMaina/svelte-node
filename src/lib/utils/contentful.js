// import { createClient } from "contentful";

// export default createClient({
//   space: process.env.CONTENTFUL_SPACE, // ID of a Compose-compatible space to be used \
//   accessToken: process.env.CONTENTFUL_ACCESSTOKEN, // delivery API key for the space \
// });
